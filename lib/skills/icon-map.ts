import { Code2, Server, Database, Cloud, Box, Zap, Palette, Shield, LucideIcon } from "lucide-react"

/**
 * Icon mapping for Notion icon names to Lucide React components
 * Used to dynamically render icons based on Notion select field values
 */
export const iconMap: Record<string, LucideIcon> = {
  Code2,
  Server,
  Database,
  Cloud,
  Box,
  Zap,
  Palette,
  Shield,
}
